<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/cream_bg"
    android:orientation="vertical">



    <!-- Body: list OR empty state, FAB overlaid -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerRabbits"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:padding="20dp"
            android:visibility="visible" />

        <!-- Empty state: shown when the user has no rabbits yet -->
        <LinearLayout
            android:id="@+id/emptyState"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="30dp"
            android:visibility="gone">

            <ImageView
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:layout_marginBottom="14dp"
                android:contentDescription="No rabbits"
                android:src="@drawable/ic_pets" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="18dp"
                android:gravity="center"
                android:text="You don't have any rabbits yet"
                android:textColor="@color/black"
                android:textSize="17sp" />

            <Button
                android:id="@+id/btnAddFirstRabbit"
                style="@style/Widget.AppCompat.Button"
                android:layout_width="wrap_content"
                android:layout_height="48dp"
                android:background="@drawable/button_register_bg"
                android:backgroundTint="@null"
                app:backgroundTint="@null"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:paddingStart="24dp"
                android:paddingEnd="24dp"
                android:stateListAnimator="@null"
                android:text="Add Your Rabbit"
                android:textAllCaps="false"
                android:textColor="@color/black" />

        </LinearLayout>

        <!-- FAB: add another rabbit (shown when list already has items) -->
        <ImageButton
            android:id="@+id/fabAddRabbit"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_gravity="bottom|end"
            android:layout_margin="24dp"
            android:background="@drawable/button_register_bg"
            android:contentDescription="Add new rabbit"
            android:elevation="6dp"
            android:padding="14dp"
            android:src="@drawable/ic_add"
            app:tint="@color/black" />

    </FrameLayout>

</LinearLayout>